

const testDataBuilder = () => {}

describe("Categorizing an extract which is", () => {

    test("Uncategorized into an empty category")
    test("Uncategorized into a category with other extracts")
    test("Uncategorized into a category with only this extract")
    test("Uncategorized into a category with this extract plus others")

    test("Categorized into the same category")
    test("Categorized into a different category")


});